{
  "name": "ost-pages",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev ./public --compatibility-date=2024-11-11",
    "deploy": "wrangler pages deploy ./public --project-name=ost-pages"
  }
}